<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.d04m06y22">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.D04m06y22">
        <activity android:name=".activity_show_details"></activity>
        <activity android:name=".showDetailsMenuActivity" />
        <activity android:name=".activity_complete_order" />
        <activity android:name=".activity_input_meal" />
        <activity android:name=".activity_input_company" />
        <activity android:name=".activity_input_worker" />
        <activity android:name=".activity_update_or_add" />
        <activity android:name=".creditsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>